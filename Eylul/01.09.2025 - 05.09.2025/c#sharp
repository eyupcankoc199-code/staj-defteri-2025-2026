Unsafe + Span<T> ile High-Performance String Reverse

Bellek yönetimi + hız odaklı. Normal string.Reverse()’den çok daha hızlıdır.

using System;

class Program
{
    unsafe static string Reverse(string input)
    {
        char[] buffer = input.ToCharArray();
        fixed (char* p = buffer)
        {
            char* left = p;
            char* right = p + buffer.Length - 1;

            while (left < right)
            {
                char tmp = *left;
                *left = *right;
                *right = tmp;
                left++;
                right--;
            }
        }
        return new string(buffer);
    }

    static void Main()
    {
        string s = "EfsaneviZorluk";
        Console.WriteLine(Reverse(s));
    }
}
